apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
spec:
  accessModes:
    - ReadWriteOnce # one node can mount it read-write
  resources:
    requests:
      storage: 100Mi # "I need 100Mi" — no storageClassName = use the default
